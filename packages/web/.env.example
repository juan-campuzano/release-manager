# API Configuration
# Base URL for the Release Manager API server
# In development, this uses the Vite proxy configured in vite.config.ts
# In production, set this to your actual API server URL (e.g., https://api.example.com)
VITE_API_BASE_URL=/api

